$(document).ready(function(){
    const prev = $('.prev');
    const next = $('.next');
    const slide = $('.slide');

    prev.on('click', function(e){
        e.preventDefault();
        slide.animate({
            
        })
    })
})